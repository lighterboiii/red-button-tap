/** Сколько ручных тапов доступно за календарный день (локальное время). */
export const MAX_TAPS_PER_DAY = 5;

/** Пауза между тапами, мс (~2 ч). */
export const TAP_COOLDOWN_MS = 2 * 60 * 60 * 1000;

/** Вероятность «дневного слота» (заглушка), раз в день при первом заходе. */
export const DAILY_BONUS_ROLL_CHANCE = 0.22;

/** Шанс, что на сегодня выпадет режим «постоянные тапы» (остальное — лимит + пауза). */
export const DAILY_MODE_ENDLESS_CHANCE = 0.45;
